# CHANGELOG - LaTeX MarkDown Converter

> **作者**：[ASH](mailto:ash_server@163.com)
> **版本**：2.0
> **更新日期**：2026/9/23

## [2.0.0] - 2026/9/23

这一版重写了转换内核，解决长文档转换后标题层级错乱、LaTeX 源码裸露的问题，
并把界面从 Tkinter 迁到 PySide6。

### 修复内容

1. **块级公式不再破坏文档结构**
   旧实现把 `$$...$$` 包装成 `<div class="katex-display-protected">` 再交给
   Markdown，当公式与正文同行、或位于列表、引用块中时会生成 `<p><div>`
   这类非法嵌套，浏览器强行闭合标签后，后续标题层级随之错乱。
   现在公式先被替换为占位符，Markdown 解析器完全接触不到 `$`。

2. **公式源码不再裸露**
   旧实现用正则直接改写原文，公式两侧缺少块级边界，行尾的 `$$` 无法被再次
   识别；公式里的 `_`、`*` 会被 Markdown 当成强调符号。现在占位符独占一行，
   且行尾闭合的 `$$` 会把剩余内容重新扫描一遍。

3. **分隔线不再吃掉标题**
   旧实现把所有 `---` 行替换成 HTML 注释占位符，而 Markdown 里
   「段落 + `---`」本来是二级标题的写法，替换后标题消失。
   现在启用 `sane_lists` 并移除该替换，`---` 稳定输出为 `<hr>`。

4. **未配对的 `$$` 保持原样**
   新增成对性检查，落单的 `$$` 不再被拆成两个行内公式。

5. **长文档渲染卡顿与失败**
   公式改为在转换阶段预渲染并内嵌 KaTeX 字体，页面不再执行渲染脚本；
   修复了 KaTeX 桥一次性写入全部请求导致的管道死锁，改为分批提交。

6. **SVG 内联更稳**
   支持单引号属性、HTML 实体、URL 编码与 `?query#fragment` 后缀；
   剥离 SVG 内的脚本与事件属性；同一文档内多个 SVG 的 id 自动加前缀，
   避免相互干扰。

7. **样式表覆盖补全**
   确认 `static/css` 下样式表为 UTF-8；补齐 Pygments 高亮类名、
   `.math-inline` / `.math-display` 公式容器、目录、页脚、
   公式出错提示与打印分页规则。

### 更新内容

1. 界面改用 PySide6：转换在后台线程执行，新增样式下拉、目录与页脚选项、
   打开结果与输出目录按钮、运行日志。
2. `main.py` 增加命令行模式（`--cli`），支持批量转换与脚本调用。
3. 新增 `tests/` 回归测试（50 项）与 `tools/verify/` 无头浏览器渲染自检。
4. 代码高亮改用 Pygments，代码块样式在深浅两套主题下都可读。

## [1.2.0] - 2026/8/23

### 更新内容

1. 将默认转化生成的 HTML 的标题由 `Markdown转换结果` 改为对应 Markdown 文件的名称。

### 修复内容

1. 优化了部分代码。

## [1.1.1] - 2026/8/22

### 修复内容

1. 修复了位于 `Line:429` 的正则表达式语法错误。
2. 修正了几处字符串内未受支持的转义符。
3. 优化了部分代码。

## [1.1.1] - 2026/8/19

### 修复内容

1. 修复了内嵌 SVG 图象时的错误转码。
2. 优化了部分代码。

## [1.1.0] - 2026/8/19

### 更新内容

1. 新增 SVG 图片自动转化功能。

### 修复内容

1. 优化了部分代码。

## [1.0.0] - 2026/8/23

### 修复内容

1. 修复了在插入图片后的 LaTeX 文本可能失效的问题。
2. 优化了部分代码。
